// LICENSE : MIT
"use strict";
// Repository to perform CRUD operations
export default class ShoppingCartRepository {
    all() { /* … */
    }

    findById(id) { /* … */
    }

    create(cart) { /* … */
    }

    update(cart) { /* … */
    }

    destroy(cart) { /* … */
    }
}
